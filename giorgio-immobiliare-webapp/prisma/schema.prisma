/**
 * Prisma is no longer used in this project.
 *
 * The app has been migrated to Cloudflare Pages + D1 + R2 and uses:
 * - D1 migrations under `giorgio-immobiliare-webapp/migrations/*.sql`
 * - Pages Functions under `giorgio-immobiliare-webapp/functions/*`
 *
 * This file is intentionally kept as a placeholder to avoid confusion if someone
 * expects a Prisma schema to exist.
 */
