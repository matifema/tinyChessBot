/**
 * NOTE:
 * `/api/listings/:id` is implemented as a Cloudflare Pages Function under `/functions/api/listings/[id]`.
 * This Next.js route handler is intentionally left as a stub to avoid route conflicts.
 *
 * If you still have this file in your repo, you should delete it.
 */
export {};
